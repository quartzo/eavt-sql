[package]
name = "spier-planner"
version = "0.1.0"
edition = "2021"
description = "Query planner (Datalog IR → Query Plan, cost-based join ordering)"

[dependencies]
spier-datalog = { path = "../spier-datalog" }
spier-query-ir = { path = "../spier-query-ir" }
spier-value = { path = "../spier-value" }
