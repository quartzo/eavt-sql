[package]
name = "spier-sql-parse"
version = "0.1.0"
edition = "2021"
description = "SQL lexer + parser (SQL string → Rust AST)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
