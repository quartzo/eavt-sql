[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "spier-eavt-query-py"
version = "0.1.0"
description = "PyO3 bindings for spier-eavt-query"
requires-python = ">=3.13"

[tool.maturin]
module-name = "spier_eavt_query_py"
manifest-path = "Cargo.toml"
