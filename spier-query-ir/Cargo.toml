[package]
name = "spier-query-ir"
version = "0.1.0"
edition = "2021"
description = "VM opcodes, instruction formats, and program handles"

[dependencies]
spier-scheme = { path = "../spier-scheme" }
spier-value = { path = "../spier-value" }
