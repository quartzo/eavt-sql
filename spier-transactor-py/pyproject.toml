[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "spier-transactor-py"
version = "0.1.0"
description = "PyO3 bindings for spier-transactor"
requires-python = ">=3.13"

[tool.maturin]
module-name = "spier_transactor_py"
manifest-path = "Cargo.toml"
