[package]
name = "spier-scheme"
version = "0.1.0"
edition = "2024"

[dependencies]
# Pure Rust — no external dependencies.
# HostFns boundary handles SExpr ↔ storage Value conversion.

[dev-dependencies]
