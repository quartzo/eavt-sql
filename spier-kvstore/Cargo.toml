[package]
name = "spier-kvstore"
version = "0.1.0"
edition = "2021"
description = "KVStore backed by Nim (blobstore + journal + memtable + page-store + transactor)"

[lib]
crate-type = ["rlib"]

[dependencies]
spier-storage-traits = { path = "../spier-storage-traits" }
spier-page-store-nim = { path = "../spier-page-store-nim" }
