[package]
name = "dynspire-commons"
version = "0.1.0"
edition = "2021"
description = "Shared traits, types, and utilities for the EAVT storage/query stack"

[features]
default = []
serde = ["dep:serde"]

[dependencies]
chrono = "0.4"
serde = { version = "1", features = ["derive"], optional = true }
