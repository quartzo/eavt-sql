[package]
name = "spier-memtable"
version = "0.1.0"
edition = "2021"
description = "MemTable write buffer (crossbeam SkipMap per CF, Arc-swap snapshots, materialized scan)"

[dependencies]
crossbeam-skiplist = "0.1"
spier-storage-traits = { path = "../spier-storage-traits" }
