[package]
name = "spier-datalog"
version = "0.1.0"
edition = "2021"
description = "Datalog IR builder (SQL AST → Datalog patterns)"

[dependencies]
serde = { version = "1", features = ["derive"] }
spier-sql-parse = { path = "../spier-sql-parse" }
spier-value = { path = "../spier-value" }
